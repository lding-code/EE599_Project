import { CodeEditorService } from './code-editor.service';
export declare class JsonDefaultsService {
    private monaco;
    constructor(codeEditorService: CodeEditorService);
    setup(monaco: any): void;
    addSchemas(id: string, definitions?: Array<{
        uri: string;
        schema: Object;
    }>): void;
}
